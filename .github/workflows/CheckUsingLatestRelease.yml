name: Check Latest Model Releases in a Qualification Plan

on:
  push:
  pull_request:

jobs:
  check-latest-releases:
    uses: Open-Systems-Pharmacology/Workflows/.github/workflows/CheckForLatestModelReleasesInQualificationPlan.yml@main